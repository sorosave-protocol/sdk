{
  "name": "@sorosave/webhook",
  "version": "1.0.0",
  "description": "Webhook notification service for SoroSave contract events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "jest --testPathPattern=webhook",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [
    "sorosave",
    "webhook",
    "stellar",
    "soroban",
    "notifications"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
